Executes shell commands in parallel on all of your nodes.

    node-control execute [help] [-s] [COMMANDS...]

COMMANDS are shell commands directly executed via ssh on all nodes.

help
    Displays this help. Remaining arguments are rendered meaningless when
    specifying this.

-s, --sequential
    Executes commands not in parallel on each node, but sequentially.
